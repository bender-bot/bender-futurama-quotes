[tox]
# note that tox expects interpreters to be found at C:\PythonXY,
# with XY being python version ("27" or "34") for instance
envlist = py27, py34

[testenv]
deps=pytest
    git+https://github.com/bender-bot/bender.git@implementing-jenkins-script
commands=py.test
